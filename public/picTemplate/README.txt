Place your photo template image here as: template.png

The template should have a solid light-blue (#ADD8E6 or similar) rectangle
marking the area where the user's camera photo will be placed.

The app will automatically detect this light-blue area and composite
the user's photo into it.
